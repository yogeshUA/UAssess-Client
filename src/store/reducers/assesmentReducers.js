import * as actionTypes from '../actionTypes';

const assesment = (state = {a:{}}, action) => {
    switch (action.type) {
        case actionTypes.GET_ASSESMENT:
            
            return{

                state
            }
            
    
 
    }
}

export default assesment;